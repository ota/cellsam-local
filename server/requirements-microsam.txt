# Optional standalone micro-sam server environment.
# Keep this isolated from the ONNX Runtime GPU and Cellpose environments.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
numpy>=1.26
opencv-python-headless>=4.10
packaging>=24
pillow>=10

# Minimal headless runtime for the registration-free micro-sam ViT-B LM weights.
# The full micro-sam package also installs GUI and tracking dependencies that are
# not needed by this server backend and currently conflict with Python 3.12.
torch>=2.5
torchvision>=0.20
segment-anything>=1.0
